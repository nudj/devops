version: '1.0'

steps:

  delete_vpc:
    image: fdearle/sol-tools
    working_directory: IMAGE_WORK_DIR
    description: Delete the VPC
    commands:
      - sol delete-stack -s service -e test -c web
      - sol delete-stack -s service -e test -c hire
      - sol delete-stack -s service -e test -c admin
      # - sol delete-stack -s service -e test -c api
      # - sol layer destroy -l db -e test
      # - sol layer destroy -l env -e test
      # - sol layer destroy -l core
