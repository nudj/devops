version: '1.0'

steps:

  executeRemote:
    image: nudj/devops:aws
    working_directory: IMAGE_WORK_DIR
    description: Create the initial VPC
    commands:
      - eval $(envkey-source)
      - ./scripts/execute-remote.sh -p evacuate-project
