version: '1.0'

steps:

  confirm:
    image: nudj/devops:aws
    working_directory: IMAGE_WORK_DIR
    commands:
      - eval $(envkey-source)
      - ./scripts/execute-remote.sh -p core-vpc-test-confirm
