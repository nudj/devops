version: '1.0'

steps:

  evacuate:
    image: nudj/devops:aws
    working_directory: IMAGE_WORK_DIR
    commands:
      - eval $(envkey-source)
      - ./scripts/execute-remote.sh -p confirm-project
